name: Production Multi-Service Monorepo Pipeline

on:
  push:
    branches:
      - main
    paths:
      - 'enterprise-devops/application/base/**'
      - 'enterprise-devops/application/overlays/**'
      - '.github/workflows/deployment-pipeline.yaml'

permissions:
  contents: write
  id-token: write

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      orders: ${{ steps.filter.outputs.orders }}
      billing: ${{ steps.filter.outputs.billing }}
      catalog: ${{ steps.filter.outputs.catalog }}
      telemetry: ${{ steps.filter.outputs.telemetry }}
      auth: ${{ steps.filter.outputs.auth }}
    steps:
      - name: Checkout Code Base
        uses: actions/checkout@v4
      - name: Paths Filter Execution
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            orders: ['enterprise-devops/application/base/order-service-*.yaml', 'enterprise-devops/application/overlays/**']
            billing: ['enterprise-devops/application/base/billing-service-*.yaml', 'enterprise-devops/application/overlays/**']
            catalog: ['enterprise-devops/application/base/catalog-service-*.yaml', 'enterprise-devops/application/overlays/**']
            telemetry: ['enterprise-devops/application/base/telemetry-service-*.yaml', 'enterprise-devops/application/overlays/**']
            auth: ['enterprise-devops/application/base/auth-service-*.yaml', 'enterprise-devops/application/overlays/**']

  build-order-service:
    needs: detect-changes
    if: needs.detect-changes.outputs.orders == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code Base
        uses: actions/checkout@v4
      - name: Authenticate to AWS via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1
          audience: ://amazonaws.com
      - name: Log into Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2
      - name: Build and Push Order Container
        run: |
          docker build -t ${{ steps.login-ecr.outputs.registry }}/order-app:${{ github.sha }} ./enterprise-devops/application/base
          docker push ${{ steps.login-ecr.outputs.registry }}/order-app:${{ github.sha }}
      - name: Update Order Version Tag
        run: |
          cd enterprise-devops/application/overlays/prod
          kustomize edit set image internal-registry.io/order-app=${{ steps.login-ecr.outputs.registry }}/order-app:${{ github.sha }}
          git config --global user.name "gitops-bot"
          git config --global user.email "gitops-bot@company.com"
          git add kustomization.yaml
          git commit -m "chore(gitops): bump order service to ${{ github.sha }} [skip ci]" || true
          git push origin main || true
